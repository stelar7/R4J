language: java
jdk: oraclejdk8
install: true
script: mvn clean install -DskipTests
cache:
  directories:
    - $HOME/.m2

deploy:
  -
    on:
      branch: release
      repo: stelar7/L4J8
    provider: github
    skip_cleanup: true
    file: target/*.jar
    user: $GIT_USER
    password: $GIT_PASS